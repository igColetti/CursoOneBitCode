const valorM = prompt("Digite um valor em metros(m2):")
const tipoM = promtp("Digite a unidade: (MM), (CM), (DM), (DAM), (HM) e (KM)")

let auxMedida = 0

switch (tipoM) {
    case MM:
        break;
    
    case CM:

    case DM:

    case DAM:

    case HM:

    case KM:

    default:
        break;
}